#pragma once

/**
 * \brief Simple 2d vector.
 * 2d vector with no other functionality. Should be replaced with class that has
 * operator overloading in the future
 */
struct Vector2
{
	long double x, y;
};